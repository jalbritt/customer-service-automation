# infrastructure/message-queue/app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: message-queue
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/your-repo/infrastructure.git'
    targetRevision: HEAD
    path: message-queue
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: message-queue
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

